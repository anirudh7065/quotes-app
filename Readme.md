# This is the third web app of the basic web development
- This is a Quote generator web app which asyncronously fetch quotes from api and display it on the web page it uses a single async function.

